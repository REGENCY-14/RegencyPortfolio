/**
 * Every hand-authored icon in src/components/icons/ shares this stroke
 * width so the logo, arrows, service icons, and process icons read as one
 * family instead of four unrelated styles.
 */
export const ICON_STROKE_WIDTH = 1.6;
